{
  "name": "reelio-backend",
  "version": "1.0.0",
  "description": "Reelio AI secure backend — calls Gemini to build an edit plan. Never exposes the API key to the client.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0"
  }
}
